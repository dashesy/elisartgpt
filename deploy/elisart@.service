[Unit]
Description=elisart server (Codex-backed art API)
After=network-online.target
Wants=network-online.target

[Service]
User=%i
WorkingDirectory=/home/%i/elisartgpt/server
EnvironmentFile=/home/%i/elisartgpt/.env
ExecStart=/home/%i/elisartgpt/server/.venv/bin/python -m uvicorn elisart.main:app --host ${ELISART_HOST} --port ${ELISART_PORT}
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
