<?xml version="1.0" encoding="utf-8"?>
<!-- Where camera shots land before upload; the camera app writes here through FileProvider. -->
<paths>
    <cache-path name="shots" path="shots/" />
    <!-- A downloaded update, handed to the package installer. -->
    <cache-path name="updates" path="updates/" />
</paths>
