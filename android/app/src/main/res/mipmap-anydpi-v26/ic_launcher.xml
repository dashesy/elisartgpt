<?xml version="1.0" encoding="utf-8"?>
<!-- Elisa's own watercolor is the icon: the face crop fills the whole tile and the
     launcher mask shapes it, so no separate foreground is needed. -->
<adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
    <background android:drawable="@drawable/ic_launcher_painting" />
    <foreground android:drawable="@android:color/transparent" />
</adaptive-icon>
