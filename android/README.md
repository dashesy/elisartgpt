# Android client (milestone 3)

Kotlin + Jetpack Compose, built with the Gradle wrapper. Planned screens:
one prompt box, the current picture, a "change it" box that continues the same
drawing, and a gallery. Server URL and token live in the app's settings, never
in the APK. Build: `make apk`.
